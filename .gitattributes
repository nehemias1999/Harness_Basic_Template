* text=auto eol=lf

# The verifiers depend on line endings: init.sh runs under bash (a CRLF breaks
# it with "bad interpreter") and init.ps1 lives on Windows.
*.sh   text eol=lf
*.py   text eol=lf
*.ps1  text eol=crlf

*.png  binary
*.jpg  binary
