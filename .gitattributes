* text=auto eol=lf

# Los verificadores dependen del final de línea: init.sh se ejecuta en bash
# (un CRLF lo rompe con "bad interpreter") y init.ps1 vive en Windows.
*.sh   text eol=lf
*.py   text eol=lf
*.ps1  text eol=crlf

*.png  binary
*.jpg  binary
